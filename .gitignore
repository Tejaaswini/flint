
# Secrets — never commit
.env.local
.env
ui/.env.local

# Demo runtime artifacts
flint-audit.jsonl
router-audit.jsonl
.demo-*.log
bin/
ui/node_modules/
ui/dist/

# Stray prebuilt binaries at repo root (use ./bin/ instead)
/control
/gateway
/flint-gateway
/flint-replay
/main

# Local dev artifacts (not for upstream)
devlogs/
.claude/
do_or_die.md
new_plan.md
plan.md

# OS
.DS_Store
